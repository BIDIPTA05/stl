#include<iostream>
#include<vector>
using namespace std;
int main(){
    vector<int> a{5,3,2,8,9,10};
    vector<int> b;

    for (int i = 0; i < a.size();i++)
    {
        b.push_back(a[i]);
    }

  for (int i = 0; i < a.size();i++)
    {
        cout << b[i] << endl;
    }


    return 0;
}
